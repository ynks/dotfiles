{
	"layer": "top",
	"position": "bottom",
	"height": 24,

	// modules
	"modules-left" : [
		"hyprland/workspaces",
		"custom/sep-gray"
	],

	"modules-right": [
		"custom/kbd-backlight",
		"backlight",
		"pulseaudio",
		"custom/net",

		"cpu",
		"temperature",
		"memory",
		"custom/gpu-profile",

		"custom/sep-orange",
		"custom/fan-profile",
		"battery",

		"custom/sep-green",
		"clock"
	],

	"custom/sep-orange": { "format": "", "tooltip": false },
	"custom/sep-gray": { "format": "", "tooltip": false },
	"custom/sep-green": { "format": "", "tooltip": false },

	// right
	"clock" : {
		"interval" : 1,
		"format" : "T {:%d %b %Y %H:%M:%S}",
		"tooltip": false
	},

	"battery" : {
		"interval" : 10,
		"states" : {
			"critical" : 20
		},

		"format" : "BAT {capacity}%",
		"format-charging" : "BAT {capacity}% CHR",
		"format-plugged" : "BAT {capacity}% PLG",
		"format-full" : "BAT FULL PLG",
		"format-critical" : "BAT {capacity}% CRITICAL",
		"format-time" : "{H}:{M}",
		"tooltip": false
	},

	"custom/fan-profile" : {
		"exec": "~/dotfiles/scripts/fan-mode.sh",
		"interval": 2,
		"format": "PROF {}",
		"tooltip": false
	},

	"custom/gpu-profile" : {
		"exec": "~/dotfiles/scripts/gpu-mode.sh",
		"interval": 10,
		"format": "GPU {}",
		"tooltip": false
	},

	"memory" : {
		"format" : "MEM {used:.1f}GiB",
		"format-critical" : "MEM {used:.1}GiB CRITICAL",
		"critical-threshold" : 80,
		"tooltip": false
	},

	"temperature" : {
		"hwmon-path": "/sys/class/hwmon/hwmon5/temp1_input",
		"format" : "TEMP {temperatureC}c",
		"critical-threshold" : 85,
		"format-critical" : "TEMP {temperatureC}c CRITICAL",
		"tooltip": false
	},

	"cpu" : {
		"format" : "CPU {load:.2f}",
		"tooltip": false
	},

	"custom/net": {
		"exec": "~/dotfiles/scripts/net.sh",
		"interval": 20,
		// "on-click" : "impala",
		"format": "NET {}",
		"tooltip": false
	},

	"pulseaudio": {
		"format": "VOL {volume}%{format_source}",
		"format-muted": "VOL mute{format_source}",
		"format-source": "",
		"format-source-muted": " mic",
		"on-click": "pavucontrol",
		"scroll-step": 5,
		"tooltip": false
	},

	"backlight": {
		"device": "amdgpu_bl1",
		"format": "SCR {percent}%",
		"tooltip": false
	},

	"custom/kbd-backlight": {
		"exec": "~/dotfiles/scripts/kbd-backlight.sh",
		"interval": 1,
		"format": "KBD {}",
		"tooltip": false
	}
}
